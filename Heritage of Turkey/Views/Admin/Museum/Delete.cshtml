@model Heritage_of_Turkey.ViewModels.Admin.MuseumAdminDetailViewModel

@{
    ViewData["Title"] = "Delete Museum";
    Layout = "_AdminLayout";
    var statusBadgeClass = Model.IsActive ? "text-bg-success" : "text-bg-secondary"; // Determine badge class based on active status
    var statusText = Model.IsActive ? "Active" : "Inactive"; // Determine status text based on active status
}

<section class="bg-white border rounded-3 shadow-sm p-4 p-lg-5">
    <div class="mb-4">
        <span class="badge rounded-pill text-bg-danger mb-3">Delete</span>
        <h1 class="h3 fw-bold mb-1">Delete Museum</h1>
        <p class="text-muted mb-0">This action permanently deletes the museum.</p>
    </div>

    <div class="alert alert-danger" role="alert">
        Are you sure you want to delete <strong>@Model.MuseumName</strong>?
    </div>

    <dl class="row gy-3">
        <dt class="col-sm-3 text-muted">Category</dt>
        <dd class="col-sm-9">@Model.CategoryName</dd>

        <dt class="col-sm-3 text-muted">City</dt>
        <dd class="col-sm-9">@Model.City</dd>

        <dt class="col-sm-3 text-muted">Status</dt>
        <dd class="col-sm-9">
            <span class="badge @statusBadgeClass">@statusText</span>
        </dd>
    </dl>

    <form asp-controller="MuseumAdmin" asp-action="Delete" asp-route-id="@Model.MuseumId" method="post">
        @Html.AntiForgeryToken()
        <div class="d-flex flex-column flex-sm-row gap-2 mt-4">
            <button type="submit" class="btn btn-danger">Delete Museum</button>
            <a asp-controller="MuseumAdmin" asp-action="Index" class="btn btn-outline-secondary">Cancel</a>
        </div>
    </form>
</section>